type Project {
  id: ID!
  name: String!
  goal: Int!
  achieved: Int!
}

type Query {
  projects: [Project!]!
  project(id: ID!): Project
}

input NewProject {
  name: String!
  goal: Int!
}

type Mutation {
  createProject(input: NewProject!): Project!
}
